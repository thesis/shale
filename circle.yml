dependencies:
  override:
    - bash ./get_selenium.sh
  cache_directories:
    - "libs/"
    - "target/"

test:
  override:
    - bash test.sh
  post:
      - bash -c "for jar in `find target | grep \"\\.jar$\" | grep -i shale` do
          cp $jar $CIRCLE_ARTIFACTS/
        done"
