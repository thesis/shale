dependencies:
  override:
    - bash ./get_selenium.sh
    - yes | lein upgrade
  cache_directories:
    - "libs/"
    - "target/"

test:
  override: []
  post:
    - bash cp_artifacts.sh $CIRCLE_ARTIFACTS/
