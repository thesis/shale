dependencies:
  override:
    - bash ./get_selenium.sh
  cache_directories:
    - "libs/"
    - "target/"

test:
  override:
    - bash test.sh
  post:
    - cp target/`ls target | grep -i shale | grep standalone | head -1` $CIRCLE_ARTIFACTS/
