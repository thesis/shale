dependencies:
  override:
    - bash ./get_selenium.sh
  cache_directories:
    - "libs/"
    - "target/"

test:
  override:
    - bash test.sh
  post:
      - bash cp_artifacts.sh $CIRCLE_ARTIFACTS/
