dependencies:
  override:
    - bash ./get_selenium.sh
    - lein upgrade
  cache_directories:
    - "libs/"
    - "target/"

test:
  override:
    - bash test.sh
  post:
      - bash cp_artifacts.sh $CIRCLE_ARTIFACTS/
